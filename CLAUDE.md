# DocKit — คู่มือสำหรับ AI ที่มาแก้โปรเจกต์นี้

@MEMORY.md

PWA จัดการเอกสารภาษาไทย (สแกน / รวม PDF / ลายน้ำ / ลายเซ็น / รูปติดบัตร / สำเนาบัตร / ปกปิดข้อมูล)
Deploy บน Vercel จาก branch `main` — **URL จริงคือ https://docket-khaki-sigma.vercel.app**
(URL อื่นที่ลงท้าย `-fancists` เป็น URL ต่อดีพลอยเมนต์เก่า ไม่อัปเดตตามโค้ด อย่าใช้เช็คผล)

---

## เริ่มทุก session ด้วยสิ่งนี้ก่อน (สำคัญที่สุด)

เจ้าของโปรเจกต์รัน Claude หลายเครื่อง/หลาย session **แต่ session ไม่แชร์ความจำกัน**
เคยเกิดกรณีสอง session แก้บั๊กเดียวกันคนละแนว แล้ว push ชนกันจนต้อง merge ย้อนหลัง

ก่อนแก้อะไร:

```bash
git pull --no-rebase        # ดึงงานที่ session อื่นทำไว้ก่อน อย่าแก้ทับ
git log --oneline -15       # ดูว่าเพิ่งมีใครแก้อะไรไป
git status                  # มีอะไรค้างอยู่ไหม
```

ถ้า `git log` โชว์ว่ามีคนแก้เรื่องเดียวกับที่กำลังจะทำ — **อ่าน diff ของเขาก่อน**
(`git show <hash>`) แล้วต่อยอด อย่าเขียนใหม่ทับ อย่า force push
ถ้า push ถูก reject ให้ merge แล้วเก็บของดีจากทั้งสองฝั่ง ไม่ใช่ `--force`

---

## Operating Rules

### NO MAGIC — ห้ามเดา
สมมติฐานต้องพูดออกมา ถ้าไม่รู้ว่าโค้ดอยู่ไฟล์ไหน ให้ค้นหา (grep) ไม่ใช่เดา path
ห้ามอ้างว่าแก้ตรงไหนแล้วถ้าไม่ได้เปิดไฟล์นั้นจริง

### VERIFY BEFORE DONE — ยังไม่เห็นผล = ยังไม่เสร็จ
"แก้ไฟล์แล้ว" ไม่ใช่เสร็จ "แก้แล้วเปิดในเบราว์เซอร์เห็นผลจริง" คือเสร็จ
โปรเจกต์นี้เป็น UI ล้วน — ต้องเทสในเบราว์เซอร์ทุกครั้ง (ดูหัวข้อ "วิธีรันและเทส")
ห้ามพูดว่า "น่าจะใช้ได้แล้ว" — ถ้าเทสไม่ได้ ให้บอกตรงๆ ว่าเทสไม่ได้เพราะอะไร

### DISSENT — เถียงก่อนลงมือ ถ้าเห็นว่าไม่เข้าท่า
เจ้าของโปรเจกต์เปิดรับการถูกค้าน (เคยถามเองว่า "หรือเราคิดผิดบอกได้")
ถ้าเขาเสนอทางที่ไม่คุ้ม/แก้ไม่ตรงจุด ให้บอกตรงๆ พร้อมเหตุผลและทางเลือกที่ดีกว่า
อย่าเออออตามเพราะเขาเป็นคนสั่ง

### SCOPE — ทำเท่าที่ขอ
ผู้ใช้มักแจ้งหลายเรื่องรวบยอดในข้อความเดียว และแทรกเรื่องใหม่กลางทาง
ให้เก็บลิสต์ให้ครบแล้วทำตามลำดับ แต่ไม่ต้องไปรีแฟกเตอร์อย่างอื่นที่ไม่ได้ขอ

### R0 / R1 / R2 — จัดระดับตามความย้อนกลับได้
- **R0** (ย้อนไม่ได้) — หยุดถามก่อน: `git push --force`, ลบ branch, ลบข้อมูลผู้ใช้, `git reset --hard`
- **R1** (ย้อนยาก) — ทำได้แต่ต้องบอกว่าทำอะไรและทำไม: `git push` ปกติ (ไป trigger deploy จริง), เปลี่ยน schema IndexedDB
- **R2** (ย้อนง่าย) — ลุยเลย: แก้โค้ด แก้ CSS เพิ่ม comment

### LEARNING CAPTURE — พลาดแล้วต้องจด
เจอบั๊กที่เสียเวลาหา หรือพลาดเรื่องเดิมซ้ำ → จดลง `MEMORY.md` 3 บรรทัด:
เกิดอะไร / ต้นเหตุ / ครั้งหน้าให้ทำอะไร (เขียนเป็นคำสั่งที่ทำตามได้ ไม่ใช่ความรู้สึก)

### SPEC-DRIVEN — อ่าน spec.md ก่อน อัปเดตหลังเสร็จ
เริ่ม session: อ่าน `spec.md` ให้รู้ว่าตอนนี้อยู่ตรงไหน
ทำเสร็จ: อัปเดต `spec.md` (สถานะล่าสุด / ตัดสินใจอะไรไป / ต่อไปทำอะไร) ก่อนบอกว่าเสร็จ

---

## สถาปัตยกรรม

**Vanilla JS ทั้งหมด ไม่มี build step ไม่มี framework ไม่มี npm** — แก้ไฟล์แล้วรีเฟรชเห็นผลเลย
อย่าเสนอให้ลง React/Vite/TypeScript โดยที่เจ้าของไม่ได้ขอ

| ไฟล์ | หน้าที่ |
|---|---|
| `index.html` | ทุกหน้าจออยู่ในไฟล์นี้ เป็น `<section class="view" id="view-xxx">` สลับด้วย `showView()` · ไอคอนเป็น SVG sprite ใน `<defs>` ด้านบน |
| `css/app.css` | design token อยู่ที่ `:root` บนสุด แก้สี/ระยะ/ความโค้งที่นั่นที่เดียว |
| `js/core.js` | state `App` (pages/sel/srcs/view), `showView`, `renderGrid`, `renderPickers`, นำเข้าไฟล์, เรขาคณิต (`convexHull`, `minAreaRect`) |
| `js/scan.js` | `applyFilter` (โหมดแต่งภาพ), `warpQuad` (ดัด perspective), `autoCorners` (หาขอบเอกสาร) |
| `js/wm.js` | ลายน้ำ + **ตัววาด overlay ลงแคนวาส** (`paintWM`/`paintObj`), `makeStampPng` (ตราคร่อม), `wmDraw` (พรีวิวสด) |
| `js/sign.js` | ลายเซ็น + กล่องวางตำแหน่ง (ลาก/ย่อขยาย/หมุน) ใช้ร่วมกับลายน้ำโหมด "วางเอง" |
| `js/photo.js` | รูปติดบัตร (ตัดพื้นหลัง flood-fill, จัดเลย์เอาต์ลงกระดาษ) |
| `js/idcard.js` | สำเนาบัตรหน้า-หลัง (หาขอบบัตร, ดัดภาพ, ตราคร่อม) |
| `js/redact.js` | ปกปิดข้อมูล — หน้า PDF ต้อง `flattenPage()` เป็นรูปก่อน ไม่ใช่แค่วางกล่องดำทับ |
| `js/export.js` | **ตัวเขียน PDF** ด้วย pdf-lib + `showDone()` (หน้าจอเสร็จแล้ว) |
| `js/history.js` | ประวัติ "ไฟล์ที่เคยสร้าง" (IndexedDB `dockit-history`) |
| `js/persist.js` | เซฟ "เอกสาร" ที่ยังทำค้าง (IndexedDB `dockit-workspace`) |
| `js/boot.js` | wiring ทั้งหมด (event listener) + `boot()` |
| `sw.js` | service worker |

โหลด script ตามลำดับใน `index.html` — ทุกอย่างเป็น global function ไม่มี module/import
**เพิ่มไฟล์ js ใหม่ ต้องเติม 2 ที่:** `<script>` ใน `index.html` และ `SHELL` ใน `sw.js`

---

## Data Contracts — ห้ามแก้ข้างเดียว

### overlay (ลายน้ำ/ลายเซ็น/กล่องดำ) ใช้พิกัด normalized 0..1
```js
{ kind:'wm'|'sig'|'redact', type:'img'|'text'|'rect',
  x, y, w, h,        // สัดส่วนของกรอบที่ตามองเห็น (0..1) y ชี้ลง
  rot,               // องศา
  opacity,           // 0..100
  dataUrl, ratio,    // เฉพาะ type 'img'
  slot }             // 'wm' | 'stamp' — แยกช่องไม่ให้ลายน้ำกับตราคร่อมทับกันเอง
```

**สัญญาข้อสำคัญที่สุดของโปรเจกต์นี้:**
ตัววาดพรีวิวลงแคนวาส (`paintWM`/`paintObj` ใน `wm.js`) กับตัวเขียนลงไฟล์ PDF
(`drawWmPdf`/`drawObjPdf` ใน `export.js`) **ต้องคำนวณตำแหน่งด้วยสูตรเดียวกัน**
แก้ข้างใดข้างเดียว = พรีวิวกับไฟล์จริงไม่ตรงกัน (บั๊กแบบที่ผู้ใช้จับได้ทันที)
แก้ที่หนึ่ง ต้องไปแก้อีกที่ให้ตรงกันเสมอ แล้วเทสว่าไฟล์ที่ส่งออกตรงกับพรีวิว

### หน้าเอกสาร (App.pages[])
```js
{ id, kind:'img'|'pdf', name, rotate, overlays:[],
  img: { blob, w, h, enh:{mode,bright,contrast}, crop },   // kind 'img'
  pdf: { srcId, idx, w, h },                               // kind 'pdf' — srcId ชี้ App.srcs
  thumb }                                                   // data URL
```

---

## กับดักที่เคยเสียเวลาไปแล้ว — อย่าเหยียบซ้ำ

1. **ห้ามเก็บ `Blob` ลง IndexedDB — เก็บเป็น `ArrayBuffer` เท่านั้น**
   Blob ใน IndexedDB บน iOS/Safari เป็นแค่ตัวชี้ไปไฟล์เบื้องหลังที่หลุดได้เอง
   object ยังอยู่แต่อ่านไบต์ไม่ได้ → `createImageBitmap` พัง → รูปเปิดไม่ขึ้น/ลายน้ำล่ม
   เก็บ `await blob.arrayBuffer()` + `mime` แล้วประกอบ `new Blob([bytes],{type})` คืนตอนอ่าน
   (ทำแล้วทั้ง `persist.js` และ `history.js` — ถ้าเพิ่มที่เก็บใหม่ ทำแบบเดียวกัน)

2. **งานที่ค้างได้แบบไม่ throw ต้องมี timeout** — `pdfjsLib.getDocument().promise` และ
   `createImageBitmap` ค้างแบบ pending ตลอดกาลได้ `try/catch` ดักไม่ได้เพราะมันไม่ throw
   ใช้ `withTimeout()` ใน `persist.js` ครอบไว้ ไม่งั้นสปินเนอร์ค้างจนกดอะไรไม่ได้ทั้งแอป

3. **`busy(true)` ต้องมี `busy(false)` ใน `finally` เสมอ** — สปินเนอร์เป็น overlay เต็มจอ
   ถ้าไม่ปิด = แอปกดไม่ได้เลย ผู้ใช้ต้องลบแอปทิ้ง

4. **ไอคอน SVG ต้องมี `viewBox="0 0 24 24"`** — sprite ไม่ได้ประกาศ viewBox ไว้
   ลืมใส่แล้วไอคอนในกรอบเล็ก (ปุ่มย้อนกลับ 15px) จะถูกตัดขอบเป็นเสี้ยว

5. **`applyFilter` โหมด `'mag'` ใช้กับ "กระดาษขาว-หมึกดำ" เท่านั้น** ห้ามใช้กับรูปถ่ายบัตร/
   รูปคน — มันเข้าใจว่าพื้นภาพคือกระดาษแล้วดันให้ขาว ทำให้สีซีดเพี้ยน

6. **ฟอนต์ไทยต้องรอโหลดก่อนวาดลงแคนวาส** — `await document.fonts.load('700 96px Sarabun')`
   ถ้าวาดตอนฟอนต์ยังไม่มา เบราว์เซอร์ใช้ฟอนต์สำรองที่วางสระ/วรรณยุกต์ผิด
   แล้วผลนั้นจะถูก cache ค้างไว้ (ดูตัวอย่าง `stampImage()` ใน `idcard.js`)

7. **ข้อความไทยลง PDF ต้องเรนเดอร์เป็นรูป ไม่ใช่ `drawText`** — pdf-lib วางสระ/วรรณยุกต์
   ซ้อนผิด (ไม่ทำ GPOS) จึงต้องผ่าน `textPng()`/`makeStampPng()` ที่ใช้ text shaper ของเบราว์เซอร์

8. **แก้ไฟล์ js/css แล้วต้องบั๊ม `V` ใน `sw.js`** (`dockit-vN` → `vN+1`)
   ไม่บั๊ม = เครื่องที่เคยเปิดแอปได้โค้ดเก่าจาก cache ผู้ใช้จะบอกว่า "แก้แล้วไม่เห็นเปลี่ยน"

---

## วิธีรันและเทส

ไม่มี build step เสิร์ฟไฟล์ static ตรงๆ:

```bash
cd /Users/vee/projects/dockit && python3 -m http.server 8791
```

เปิด `http://localhost:8791` (มือถือในวง Wi-Fi เดียวกันใช้ IP ของเครื่อง — จำเป็นถ้าจะเทสกล้อง)

**เบราว์เซอร์ cache ไฟล์ js เหนียวมาก** ถ้าแก้โค้ดแล้วเทสไม่เห็นผล ให้เสิร์ฟพอร์ตใหม่
(8792, 8793, …) จะชัวร์กว่านั่งกด hard reload — เสียเวลาไปเยอะกับเรื่องนี้แล้ว
(service worker ไม่ลงทะเบียนบน localhost อยู่แล้ว ตั้งใจให้เป็นแบบนั้น)

เทสโดยไม่ต้องมีไฟล์จริง: สร้างหน้าเอกสารปลอมด้วยแคนวาสแล้ว push เข้า `App.pages`
```js
const cv = document.createElement('canvas'); cv.width=400; cv.height=560;
const ctx = cv.getContext('2d'); ctx.fillStyle='#fff'; ctx.fillRect(0,0,400,560);
const blob = await new Promise(r => cv.toBlob(r,'image/jpeg',0.9));
const p = { id: uid(), kind:'img', name:'test.jpg', rotate:0, overlays:[],
  img:{ blob, w:cv.width, h:cv.height, enh:{mode:'orig',bright:0,contrast:0}, crop:null }, thumb:null };
App.pages.push(p); await refreshPageRaster(p); renderGrid();
```

เช็คไฟล์ PDF ที่ส่งออกจริง (ไม่ใช่แค่พรีวิว) ให้เรนเดอร์กลับด้วย pdf.js แล้วดูภาพ —
บั๊กตำแหน่ง/ตัวอักษรขาดมักโผล่เฉพาะในไฟล์จริง ไม่โผล่ในพรีวิว

---

## การสื่อสาร

- **ตอบเป็นภาษาไทย** เจ้าของโปรเจกต์คุยไทย
- ตรงประเด็น บอกว่าแก้อะไรที่ไฟล์ไหนเพราะอะไร ไม่ต้องเยิ่นเย้อ
- เจอบั๊กที่ไม่ได้ถูกแจ้ง ให้บอก — เขาอยากรู้ (หลายบั๊กในโปรเจกต์นี้เจอแบบนั้น)
- ถ้าเทสแล้วไม่พบปัญหาที่เขาแจ้ง **ให้บอกตรงๆ ว่าไม่พบ พร้อมบอกว่าเทสอะไรไป**
  แล้วเดาสาเหตุอื่น (เช่น cache) — อย่าแกล้งแก้อะไรไปมั่วๆ เพื่อให้ดูเหมือนทำงาน
- ยังไม่ commit/push จนกว่าจะบอก และบอกด้วยว่า push แล้วจะไป trigger deploy จริง
- git identity ของ repo นี้: `Apinya CH <apinya.ch@cjmart.co.th>`
