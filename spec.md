# DocKit — PWA จัดการเอกสารภาษาไทย ใช้ได้ออฟไลน์ ไม่ส่งไฟล์ขึ้นเซิร์ฟเวอร์

ทุกอย่างประมวลผลในเครื่องผู้ใช้ (canvas + pdf.js + pdf-lib) ไม่มี backend
เป้าหมาย: งานเอกสารที่คนไทยทำบ่อย — สแกน, สำเนาบัตร, รูปติดบัตร, ลายน้ำ/คร่อมสำเนา, ลายเซ็น

## Architecture

Vanilla JS ล้วน **ไม่มี build step / npm / framework** เสิร์ฟไฟล์ static ตรงๆ
Deploy: Vercel auto-deploy จาก `main` → https://docket-khaki-sigma.vercel.app
รายละเอียดไฟล์และ data contract ดูใน `CLAUDE.md`

เครื่องมือแบ่ง 2 กลุ่มที่ทำงานต่างกัน:
- **กลุ่มที่ใช้ "หน้าเอกสาร" ร่วมกัน** (สแกน → ลายน้ำ → ลายเซ็น → ปกปิดข้อมูล → ส่งออก)
  แปะเป็นเลเยอร์ซ้อนบนหน้าเดิม ทำต่อเนื่องกันได้ ยังไม่ "เผา" ลงไฟล์จนกดส่งออก
- **กลุ่มเดี่ยวจบในตัว** (รูปติดบัตร, สำเนาบัตร) ไม่ผูกกับหน้าเอกสาร กดสร้างได้ PDF เลย
  มีปุ่ม "ใส่ลายน้ำต่อ" ในหน้าเสร็จแล้ว เผื่ออยากส่งเข้ากลุ่มแรก

ที่เก็บข้อมูล 3 ที่ แยกหน้าที่กันชัดเจน (IndexedDB ทั้งหมด เก็บเป็น ArrayBuffer ไม่ใช่ Blob):
- `dockit-workspace` (`persist.js`) — "เอกสาร" ที่ยังทำค้าง แก้ต่อได้ กู้คืนตอนเปิดแอปใหม่
- `dockit-history` (`history.js`) — "ไฟล์ที่เคยสร้าง" ที่ส่งออกเสร็จแล้ว เก็บล่าสุด 30 ไฟล์
- `dockit-library` (`library.js`) — "คลังของฉัน" ของที่ผู้ใช้ตั้งใจเก็บถาวร จัดเป็นโฟลเดอร์

**หน้าเอกสาร (`App.pages`) คือโต๊ะทำงานกลาง ไม่ใช่ที่เก็บไฟล์** — ไฟล์เข้ามาได้ 7 ทาง
(แถบนำเข้าในแท็บเอกสาร · ปุ่ม "จากไฟล์ที่เคยสร้าง" · กล่อง `needFiles` ที่เด้งเมื่อเปิดเครื่องมือ
ที่ต้องมีหน้าก่อน · ไทล์หน้าแรกสแกน/รวม PDF · ปุ่ม "+" ในตัวเลือกหน้าของเครื่องมือ · คลังของฉัน →
"ใช้เป็นหน้าเอกสาร" · ปุ่ม "ใส่ลายน้ำต่อ" ในหน้าเสร็จแล้วของเครื่องมือเดี่ยว) ทุกทางต้องลงที่
`addFiles()` ใน `core.js` เท่านั้น — อย่าเรียก `addPdfFiles()`/`addImageFiles()` ตรงจากที่ที่
ไม่รู้ชนิดไฟล์ล่วงหน้า (เคยเป็นบั๊กมาแล้ว ดูรอบ 2026-08-26)

## Done

**ฟีเจอร์หลัก** — สแกน/แต่งภาพ (ตัดขอบ+ดัด perspective+ปรับแสง), รวม PDF, ลายน้ำ 3 แบบ
(จาง/คร่อมสำเนา/รูปโลโก้) × 4 การจัดวาง, ลายเซ็น (วาด/อัปโหลด/บันทึกไว้ใช้ซ้ำ),
รูปติดบัตร (4 ขนาดมาตรฐาน + ตัดพื้นหลัง + จัดลงกระดาษ), สำเนาบัตรหน้า-หลังขนาดเท่าจริง,
ปกปิดข้อมูล (flatten เป็นรูปก่อนเพื่อลบข้อความจริง), ส่งออก PDF (ขนาด/คุณภาพ/เลขหน้า/หัวกระดาษ)

**รอบล่าสุด (2026-08-24 → 25)**

*จาก 4 เรื่องที่ผู้ใช้แจ้งตอนแรก*
- พรีวิวลายน้ำสดก่อนกดใส่จริง (`wmDraw`) — เดิมไม่เห็นเลยว่าจะออกมาแบบไหน
- ตัดขอบบัตรตอนถ่ายเอียงแม่นขึ้น — เปลี่ยนจากหาจุดสุดขั้วตามแนวทแยง (ไวต่อเงา/แสงสะท้อน)
  มาเป็น convex hull + minimum-area rect (rotating calipers) · เทสเอียง 18° คลาดสัดส่วน <0.2%
- ประวัติ "ไฟล์ที่เคยสร้าง" (`history.js` ใหม่) — เดิมทำเสร็จแล้วไฟล์หายไปเลย
- รูปติดบัตร: เพิ่ม color picker เลือกพื้นหลังเอง (เดิมมีแค่ 4 สีสำเร็จ) + hint บอกว่าลากเลื่อนได้

*บั๊กที่เจอเพิ่มระหว่างทาง*
- ตราคร่อมสำเนาเดิมลอยอยู่ในช่องว่างระหว่างบัตร ไม่ทับรูปเลย → แก้ให้คร่อมทับรอยต่อจริง
- ตัวอักษรไทยขาดเพราะวาดตอนฟอนต์ Sarabun ยังโหลดไม่เสร็จแล้วผลถูก cache ค้าง
- ไอคอน SVG ทั้งแอปไม่มี `viewBox` → ปุ่มย้อนกลับ (15px) แสดงลูกศรเป็นเสี้ยว
- รูปอัปโหลดซีดจาง เพราะ `renderCard` เอา `applyFilter` โหมด `'mag'` (สำหรับกระดาษขาว-หมึกดำ)
  ไปใช้กับรูปถ่ายบัตร → ตัดออก
- **แอปค้างสปินเนอร์เปิดไม่ขึ้น** (ร้ายแรงสุด) — ต้นเหตุ: เก็บ `Blob` ลง IndexedDB ซึ่งบน
  iOS/Safari เป็นตัวชี้ไฟล์ที่หลุดได้ → `createImageBitmap` พัง + งาน pending ตลอดกาลทำให้
  `busy(false)` ไม่ถูกเรียก แก้: เก็บ ArrayBuffer, `withTimeout()`, `try/finally`, แตะสปินเนอร์
  ค้าง 2 วิเพื่อยกเลิก (รอบแรกแก้แค่ปลายทาง — ตรวจเจอแล้วทิ้งหน้า ซึ่งข้อมูลผู้ใช้หาย)

*UX/เวิร์กโฟลว์ (จากที่ผู้ใช้บอกว่า sequence งงๆ)*
- ป๊อปอัพ "ทำอะไรต่อดี?" หลังใส่ลายน้ำ/ลายเซ็น/ปกปิดข้อมูล/สแกนเสร็จ — เดิมทำเสร็จแล้ว
  ไม่รู้จะไปต่อเครื่องมืออื่นยังไง ต้องกลับหน้าแรกเอง
- ปุ่ม "+" เพิ่มหน้าเอกสารได้จากในตัวเลือกหน้าของแต่ละเครื่องมือเลย ไม่ต้องออกไปแท็บเอกสาร
- "รวมไฟล์ PDF" และ "สแกนเอกสาร" เลือกจากไฟล์ในเครื่อง/ประวัติได้ (เดิมสแกนบังคับเปิดกล้อง)
- เซฟ "เอกสาร" ที่ทำค้างลง IndexedDB — เดิมรีเฟรชแล้วหายหมด
- หน้าแรกแยกหมวด "เอกสารประจำตัว" (รูปติดบัตร/สำเนาบัตร) ออกจากเครื่องมือทั่วไป
- แถบเลือกหน้า: "หมุนที่เลือก/ลบที่เลือก" + เทาเมื่อยังไม่เลือก + เส้นคั่นแยกกลุ่มปุ่ม
- จับหมุนลายน้ำ/ตราคร่อมได้ (เพิ่มจุดจับหมุนในหน้าวางตำแหน่ง เดิมมีแค่ลาก/ย่อขยาย)
- เพิ่มขนาด/คุณภาพรูปที่อัปโหลดทุกจุด (2400→3200px ทั่วไป, บัตร 2200→2800, รูปติดบัตร 2000→2600)

**รอบ 2026-08-26**
- คลังของฉัน (`library.js` ใหม่, IndexedDB `dockit-library`) — เก็บไฟล์ที่ใช้บ่อยเป็นโฟลเดอร์
  หยิบเข้าหน้าเอกสารหรือเข้าเครื่องมือสำเนาบัตรได้เลย · รูปติดบัตรเพิ่มขนาดกำหนดเอง +
  ส่งออกเป็น JPG/PNG ได้ (ไม่ใช่แค่ PDF)
- **แก้บั๊กที่มาจากของใหม่สองอันนั้น:** ทางที่ดึงไฟล์กลับเข้าหน้าเอกสารยังเรียก `addPdfFiles()`
  ตรงๆ ทั้งที่ประวัติมีรูป (JPG/PNG) ปนแล้ว → เลือกไฟล์รูปจากประวัติ หรือกด "ใส่ลายน้ำต่อ"
  หลังส่งออกรูปติดบัตรเป็น JPG แล้วขึ้น "อ่าน PDF ไม่สำเร็จ" · และเพราะ `throw` หลุดออกจาก
  ลูปทั้งก้อน ไฟล์ PDF ที่เลือกมาพร้อมกันก็ไม่ถูกเพิ่ม แถม `renderGrid()` ไม่ถูกเรียก
  หน้าที่เข้ามาแล้วจึงไม่โผล่ในตะแกรงและไม่ถูกเซฟลง workspace
  แก้: เพิ่ม `addFiles()` เป็นประตูเดียว (แยกชนิดจาก mime แล้ว fallback นามสกุล เพราะ blob
  ที่ผ่าน IndexedDB มา `type` อาจว่าง) · ย้าย try/catch ของ `addPdfFiles`/`addImageFiles`
  เข้าไปในลูปต่อไฟล์ ไฟล์เสียใบเดียวไม่ล้มทั้งชุด · สรุปผลเป็น toast อันเดียวบอกชื่อไฟล์ที่อ่านไม่ได้
  · `busy(false)` ย้ายไป `finally` ทั้งสองตัว
  เทสด้วย Playwright/Chromium 6 เคส: ประวัติ PDF+JPG+PNG ปนกัน, ปุ่มใส่ลายน้ำต่อจาก JPG,
  ไฟล์เสียคากลางชุด, สลับชนิด+mime ว่าง (ลำดับต้องคงเดิม), ทางเดิมที่ไม่ได้แตะ

- **แท็บเอกสาร: เลือกหน้าแล้วส่งไปทำต่อได้** — เดิมเลือกแล้วมีแค่ "หมุน/ลบ" ผู้ใช้จึงไม่เห็นว่า
  เลือกไปทำอะไร ทั้งที่ `App.sel` เป็นตัวเดียวกับ "เลือกหน้า" ในเครื่องมือ (`scopePages`)
  แค่ไม่มีทางไปจากหน้านี้ · เพิ่มแถว "ลายน้ำ / ลายเซ็น / ปกปิด / ส่งออก" ที่โผล่พร้อมแถบคำสั่ง
  กดแล้วตั้ง scope ของเครื่องมือเป็น "เลือกหน้า" ให้เลย (`selectSeg` รู้จัก `data-sc` แล้ว)
  ปุ่มยุบให้ลงแถวเดียวบนจอ 390px (`.chips.tight`) ไม่ให้แถบควบคุมกินที่เหนือตะแกรง
- "เลือกหมด / ล้าง" เปลี่ยนเป็นสไตล์เบา (`.chip-btn.quiet` — พื้นใส เส้นจาง ตัวอักษรมัวท์)
  เพราะเป็นตัวคุมการเลือก ไม่ใช่คำสั่งที่ทำอะไรกับหน้า จะได้ไม่แข่งสายตากับ หมุน/ลบ/ทำต่อ

**ตัดสินใจแล้ว ไม่ทำ**
- ไม่รวมแท็บ "เอกสาร" กับ "ไฟล์ที่เคยสร้าง" เป็นอันเดียว — คนละสถานะข้อมูล
  (อันหนึ่งแก้ต่อได้ อีกอันเป็น PDF แบนแล้ว) แค่ทำให้เชื่อมกันชัดขึ้น
- ไม่ทำ log ประวัติการแก้ไขระดับ undo/redo — ผู้ใช้เลือกแบบ "เก็บไฟล์จริงไว้โหลดซ้ำ" แทน
- คลังของฉันเป็นที่เก็บ "ขาเข้า" อย่างเดียวโดยตั้งใจ (เพิ่มไฟล์ได้จากในโฟลเดอร์เท่านั้น)
  ยังไม่มีปุ่ม "เก็บเข้าคลัง" จากหน้าเอกสาร/ประวัติ — รอดูว่าผู้ใช้ต้องการจริงไหมก่อน

## Todo / Out of scope

**ยังไม่ได้ทำ / รอผู้ใช้ยืนยัน**
- ผู้ใช้แจ้งว่าตราคร่อม "ข้อความไม่ครบ" — เทสในเครื่อง (รวมเรนเดอร์ PDF จริงกลับมาดู)
  แล้วขึ้นครบทุกบรรทัด ยังสร้างอาการซ้ำไม่ได้ **รอสกรีนช็อตจากเครื่องผู้ใช้ว่าขาดตรงไหน**
  ตั้งสมมติฐานว่าเป็น sw cache เก่า (บั๊ม `V` เป็น v7 แล้ว)

**Out of scope — อย่าเผลอไปทำ**
- ไม่มี backend / ไม่อัปโหลดไฟล์ขึ้นเซิร์ฟเวอร์ (จุดขายคือความเป็นส่วนตัว)
- ไม่ลง framework/build step (React/Vite/TS) — ตั้งใจให้แก้ไฟล์แล้วรีเฟรชเห็นผลเลย
- ไม่ทำ OCR / ไม่ใช้โมเดล AI ตัดพื้นหลัง (ใช้ flood-fill พอ)
- ไม่ทำระบบ user/login/sync ข้ามเครื่อง

## Current state

ล่าสุดอยู่บน branch `claude/document-page-concept-3sircm` (ยังไม่ merge เข้า `main`)
`sw.js` บั๊มเป็น `dockit-v18` แล้วตามที่แก้ไฟล์ js/css

ก่อนหน้านี้ `main` อยู่ที่ `04e73c6` (คลังของฉัน + รูปติดบัตร JPG/PNG)

**ยังค้างจากรอบก่อน:** ผู้ใช้แจ้งตราคร่อม "ข้อความไม่ครบ" บน iPhone — แก้ที่ต้นเหตุแล้ว
(`ctx.textAlign` ที่ Safari/iOS ไม่ทำตาม, commit `a719817`) แต่ยังไม่มีคำยืนยันจากเครื่องผู้ใช้

**ขั้นต่อไป:** รอผู้ใช้ยืนยันบน https://docket-khaki-sigma.vercel.app ว่า
(1) เลือกไฟล์รูปจากประวัติ/กด "ใส่ลายน้ำต่อ" หลังส่งออก JPG ใช้ได้แล้ว
(2) ตราคร่อมบน iPhone ขึ้นข้อความครบ
(3) แถว "ส่งหน้าที่เลือกไปทำต่อ" ในแท็บเอกสารตรงกับที่ต้องการ (ยังไม่ merge เข้า main)
(ถ้าเป็น PWA ที่ติดตั้งไว้ ให้ปิดเปิดใหม่เพื่อรับ sw v16)
