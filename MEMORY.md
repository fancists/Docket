# MEMORY — บทเรียนจากที่พลาดมาแล้ว

จดเมื่อ: เจอบั๊กที่เสียเวลาหา / พลาดเรื่องเดิมซ้ำ / เจ้าของโปรเจกต์ต้องมาแก้คำตอบ
รูปแบบ: **เกิดอะไร / ต้นเหตุ / ครั้งหน้าให้ทำอะไร** (เขียนเป็นคำสั่งที่ทำตามได้)

---

### session แยกกันแก้บั๊กเดียวกันคนละแนว (2026-08-25)
- **เกิดอะไร:** สอง Claude session (คนละเครื่อง บัญชีเดียวกัน) แก้บั๊กแอปค้างเดียวกัน
  คนละวิธี แล้ว push ชนกัน ต้อง merge ย้อนหลัง เสียเวลาและเกือบทับงานกันทิ้ง
- **ต้นเหตุ:** ไม่ได้ `git pull` / อ่าน `git log` ก่อนลงมือ เลยไม่รู้ว่ามีคนแก้ไปแล้ว
- **ครั้งหน้า:** เริ่มทุก session ด้วย `git pull --no-rebase && git log --oneline -15`
  ถ้าเห็นว่ามีคนแก้เรื่องเดียวกัน ให้ `git show <hash>` อ่านก่อนแล้วต่อยอด ห้าม force push

### แก้ปลายทาง ไม่ได้แก้ต้นเหตุ (Blob ใน IndexedDB) (2026-08-25)
- **เกิดอะไร:** `createImageBitmap` พังหลังรีโหลด รอบแรกแก้ด้วยการ "ตรวจเจอ blob เสีย
  แล้วทิ้งหน้านั้น" ซึ่งกันแอปพังได้แต่ข้อมูลผู้ใช้หาย และปัญหาเกิดซ้ำเรื่อยๆ
  ผู้ใช้ต้องมาบอกว่า "ยังมีปัญหาอยู่เลย เหมือนที่เค้าทำมันไม่ได้แก้"
- **ต้นเหตุ:** เก็บ `Blob` ลง IndexedDB ตรงๆ — บน iOS/Safari มันเป็นแค่ตัวชี้ไปไฟล์
  เบื้องหลังที่หลุดได้เอง object ยังอยู่แต่อ่านไบต์ไม่ได้
- **ครั้งหน้า:** เจออาการ "ข้อมูลกลับมาแล้วแต่ใช้ไม่ได้" ให้ถามก่อนว่า *ทำไมมันเสีย*
  ไม่ใช่ *จะตรวจเจอตอนเสียได้ยังไง* · และห้ามเก็บ Blob ลง IndexedDB ใช้ ArrayBuffer เสมอ

### สปินเนอร์ค้างทำแอปใช้ไม่ได้ทั้งตัว (2026-08-25)
- **เกิดอะไร:** ผู้ใช้เปิดแอปแล้วสปินเนอร์หมุนค้าง กดอะไรไม่ได้เลย และค้างซ้ำทุกครั้ง
  ที่เปิดใหม่ (เพราะสถานะเสียถูกเซฟไว้ใน IndexedDB แล้ว)
- **ต้นเหตุ:** `busy(true)` แล้ว `await` งานที่ค้างแบบ pending ตลอดกาล (`getDocument`,
  `createImageBitmap`) — `try/catch` ดักไม่ได้เพราะไม่ throw จึงไม่ถึง `busy(false)`
- **ครั้งหน้า:** `busy(true)` ต้องคู่กับ `busy(false)` ใน `finally` เสมอ · งาน async ที่พึ่ง
  I/O ภายนอกให้ครอบ `withTimeout()` · คิดเผื่อกรณี "สถานะเสียถูกเซฟแล้ว = brick ถาวร"

### เทสแล้วเห็นโค้ดเก่า เพราะเบราว์เซอร์ cache (2026-08-25)
- **เกิดอะไร:** แก้โค้ดแล้วเทสในเบราว์เซอร์ ผลออกมาเหมือนเดิม เกือบสรุปผิดว่าแก้ไม่ได้ผล
  (`updateHeader.toString()` ยังโชว์โค้ดเวอร์ชันเก่า)
- **ต้นเหตุ:** เบราว์เซอร์ cache ไฟล์ js เหนียว reload ธรรมดาไม่พอ
- **ครั้งหน้า:** ถ้าผลเทสขัดกับโค้ดที่เพิ่งแก้ ให้สงสัย cache ก่อนสงสัยตัวโค้ด
  วิธีชัวร์: เสิร์ฟพอร์ตใหม่ (8792, 8793, …) แล้วเปิดใหม่

### สรุปว่า "ไม่พบปัญหา" ทั้งที่ผู้ใช้เจอจริง (2026-08-25)
- **เกิดอะไร:** ผู้ใช้บอกตราคร่อมข้อความไม่ครบ เทสในเครื่องแล้วขึ้นครบ จึงตอบว่าโค้ดถูกแล้ว
  น่าจะเป็น cache — แต่ปัญหาจริงยังอยู่
- **ต้นเหตุ:** เทสบน localhost ที่ไม่มี service worker + สภาพเครื่องผู้ใช้ต่างจากเครื่องเทส
  (PWA ที่ติดตั้งแล้ว, iOS, cache เก่า) เลยสร้างอาการซ้ำไม่ได้
- **ครั้งหน้า:** เทสไม่พบ ≠ ไม่มีบั๊ก · บอกตรงๆ ว่าเทสอะไรไปแล้วไม่พบ พร้อมขอข้อมูลเพิ่ม
  (สกรีนช็อต/รุ่นเครื่อง) และไปดูปัจจัยสภาพแวดล้อม (sw cache) แทนการเดาแก้โค้ดมั่ว

### สรุปว่า "ตราคร่อมแสดงครบแล้ว" เพราะเทสด้วยค่าเริ่มต้น (2026-08-25)
- **เกิดอะไร:** ผู้ใช้แจ้งว่าตราคร่อมข้อความไม่ครบ เทสด้วยข้อความสั้น+ขนาดเริ่มต้นแล้วครบ
  จึงตอบว่าโค้ดถูกแล้ว น่าจะเป็น cache — ผู้ใช้กลับมาบอกซ้ำว่า "ไม่ครบอีกแล้ว"
  ต้นเหตุจริง: `wmImgSize()` / ลายน้ำข้อความ ไม่มีการหนีบขนาดให้พอในหน้ากระดาษ
  ดันสไลเดอร์ "ขนาด" ขึ้นแล้วภาพกว้างเกินหน้า (size 200 = 1.4 เท่าของความกว้างหน้า)
  ข้อความจึงถูกตัดหาย · ลายน้ำข้อความยาวๆ ล้นตั้งแต่ขนาดเริ่มต้นเลย (648px บนหน้า 400px)
- **ต้นเหตุ (ของการวินิจฉัยพลาด):** เทสด้วยค่า default ค่าเดียว ไม่ได้กวาดช่วงค่าที่ผู้ใช้ปรับได้
- **ครั้งหน้า:** บั๊กที่ขึ้นกับค่าที่ผู้ใช้ปรับได้ ให้เทสหลายค่ารวมค่าสุดขั้ว (min/max ของสไลเดอร์)
  และข้อความทั้งสั้น/ยาว ก่อนสรุปว่า "ไม่พบปัญหา"

### เข้าใจผิดว่า Vercel มีสองโปรเจกต์ (2026-08-25)
- **เกิดอะไร:** อ่านสกรีนช็อต Vercel ผิด สรุปว่ามีโปรเจกต์ `dockit` กับ `docket` แยกกัน
  แล้วให้ผู้ใช้ไปแก้ config ที่ไม่มีอยู่จริง
- **ต้นเหตุ:** เดาจากชื่อ URL แทนที่จะขอดูหน้า Domains ของโปรเจกต์ให้ชัดก่อน
- **ครั้งหน้า:** URL ที่ Vercel สร้างต่อดีพลอยเมนต์ (มีคำสุ่มต่อท้าย) ไม่อัปเดตตามโค้ด
  โดเมนจริงต้องดูที่หน้า project → Domains · ของโปรเจกต์นี้คือ
  `docket-khaki-sigma.vercel.app` — ใช้อันนี้เช็คผล deploy เท่านั้น

### ตราคร่อมไม่คาบบัตรบนกระดาษใหญ่ (เจอจาก full test) (2026-08-25)
- **เกิดอะไร:** `stampBox()` ตั้งใจให้ตราสูงกว่าช่องว่าง (gap*1.35) เพื่อคาบบัตรทั้งสองด้าน
  แต่พอข้อความยาว ตราจะแบนมาก การหนีบความกว้างไม่ให้ล้นกระดาษทำให้ความสูงหดต่ำกว่า gap
  ตราจึงไปนั่งในช่องว่างเฉยๆ ไม่ทับบัตร — พลาดบน A4 (gap 31.5 มม.) และ Letter, ผ่านบน A5
  แบบฉิวเฉียด (จึงเทสด้วยกระดาษเดียวแล้วไม่เจอ)
- **ต้นเหตุ:** มีข้อจำกัดสองอย่างขัดกัน (กว้างไม่เกินกระดาษ / สูงกว่าช่องว่าง) โดยไม่มีใคร
  ยอมใคร — กระดาษยิ่งใหญ่ gap ยิ่งกว้าง ยิ่งขัดกันหนัก
- **ครั้งหน้า:** เพิ่ม `idGapCapFor()` บีบ gap ตามความแบนของตรา และรวมเรขาคณิตไว้ที่
  `idGeom()` ให้พรีวิวกับตัวเขียน PDF ใช้ชุดเดียวกัน · บทเรียน: ฟีเจอร์ที่ผลลัพธ์ขึ้นกับ
  "ขนาดกระดาษ × ความยาวข้อความ" ต้องเทสเป็นเมทริกซ์ ไม่ใช่ค่าเดียว

### "ตัวหนังสือไม่ครบ" ที่จริงคือพรีวิวความละเอียดไม่พอ (2026-08-25)
- **เกิดอะไร:** ผู้ใช้ส่งภาพมาว่าตราคร่อมขึ้น "ใช้สำหรับเปิดบัญฺ" (ตัว "ชี" หาย) ไล่เทสแล้ว
  พบว่า: ภาพตราคร่อม (PNG) ไม่ถูกตัดเลย 12/12 เคส · ฟอนต์เรนเดอร์ครบทั้ง Sarabun และ
  ฟอนต์สำรอง · แคนวาสไม่ล้นจอ — แต่ปัญหามีจริง
- **ต้นเหตุ:** แคนวาสพรีวิวตั้ง `cv.width = W` เท่า CSS pixel โดยไม่คูณ devicePixelRatio
  บนจอ 2x/3x จึงเรนเดอร์ที่ครึ่ง/หนึ่งในสามของความละเอียดจริง สระ/วรรณยุกต์ไทยและ
  ตัวอักษรบางเลยเบลอหายไปจนดู "ไม่ครบ" (ไฟล์ PDF ที่ส่งออกครบและคมชัดตลอด)
- **ครั้งหน้า:** แคนวาสพรีวิวทุกตัวต้องผ่าน `fitCanvas()` (คูณ DPR + setTransform)
  · บทเรียน: อาการ "ตัวอักษรหาย" บนจอความละเอียดต่ำ ให้สงสัยความละเอียดการเรนเดอร์
  ก่อนสงสัยว่าข้อความถูกตัด — วัดกล่องหมึกในภาพเพื่อแยกสองเรื่องนี้ออกจากกัน

### Safari/iOS เมิน ctx.textAlign ทำตราคร่อมข้อความขาด (2026-08-25)
- **เกิดอะไร:** ตราคร่อมบน iPhone ข้อความขาดท้าย ("ใช้ประกอบสัญ" แทน "ใช้ประกอบสัญญาเท่านั้น")
  ผู้ใช้แจ้งซ้ำหลายรอบ เทสบน Chrome ไม่เจอเลย ผมเดาผิดไป 3 รอบ (คิดว่าเป็น cache,
  ความละเอียดพรีวิว, แล้วก็ DPI ของภาพ)
- **ต้นเหตุ:** `ctx.textAlign = 'center'` ไม่มีผลบน Safari/iOS ของผู้ใช้ ข้อความจึงถูกวาด
  จากกลางภาพไปทางขวา (แบบ left-align) แล้วตกขอบขวาหาย · พิสูจน์จากไฟล์จริง: แกะภาพ
  ที่ฝังใน PDF ออกมาวัด ตัวอักษรอยู่ x 534..1081 ของภาพกว้าง 1082 (ขอบซ้าย 534 ขอบขวา 0)
  ขณะที่เครื่องผมรันโค้ดเดียวกันได้ 42..1029 (จัดกลางถูก)
- **ครั้งหน้า:** ห้ามพึ่ง `ctx.textAlign`/`textBaseline` ในงานที่ผลลัพธ์ถูกฝังลงไฟล์
  ให้คำนวณตำแหน่งเองจาก `measureText` ของคอนเท็กซ์ตัวที่วาดจริง (ได้ผลเท่ากันทุกเอนจิน
  และแก้ปัญหาวัดคนละฟอนต์กับที่เรนเดอร์ไปด้วย)
- **วิธีดีบักที่ได้ผล:** ขอไฟล์จริงจากผู้ใช้ แล้วแกะ PDF ด้วย Python (zlib) เอาภาพที่ฝัง
  ออกมาวัดกล่องหมึกเทียบขอบภาพ + เขียนเป็น PNG แล้วเปิดดูด้วยตา · และจำลองบั๊กของเบราว์เซอร์
  ด้วยการ override property (`Object.defineProperty` ทับ setter ให้เมินค่า) เพื่อเทสว่าแก้จริง

### เพิ่มชนิดไฟล์ขาออกใหม่ แต่ไม่ตามไปดูทางขาเข้า (2026-08-26)
- **เกิดอะไร:** commit ก่อน (`04e73c6`) ทำให้รูปติดบัตรส่งออกเป็น JPG/PNG ได้ ไฟล์นั้นเข้าประวัติด้วย
  แต่ทางที่ดึงไฟล์จากประวัติ/หน้าเสร็จแล้วกลับเข้าหน้าเอกสารยังเรียก `addPdfFiles()` ตรงๆ
  → เลือกไฟล์รูปจากประวัติแล้วขึ้น "อ่าน PDF ไม่สำเร็จ" · แถม `throw` หลุดออกจากลูปทั้งก้อน
  ไฟล์ PDF ที่เลือกมาพร้อมกันก็หาย และ `renderGrid()` ไม่ถูกเรียก หน้าที่เข้ามาแล้วเลย
  ไม่โผล่ในตะแกรงและไม่ถูกเซฟลง workspace (สถานะผี)
- **ต้นเหตุ:** มีหลายทางที่ป้อนไฟล์เข้า `App.pages` แล้วแต่ละทางเดาชนิดไฟล์เอง
  บางทางเช็ค mime บางทางไม่เช็คเลย พอเพิ่มชนิดไฟล์ใหม่ที่ปลายทางออก จึงมีทางที่ตกหล่น
- **ครั้งหน้า:** ทางเข้าข้อมูลชนิดเดียวกันต้องมีประตูเดียว — ใช้ `addFiles()` ใน `core.js` เท่านั้น
  ห้ามเรียก `addPdfFiles()`/`addImageFiles()` ตรงจากที่ที่ไม่รู้ชนิดไฟล์ล่วงหน้า ·
  เพิ่มชนิดไฟล์ที่ "ขาออก" ให้ `grep` หาทุกที่ที่รับไฟล์ "ขาเข้า" ก่อนปิดงาน ·
  ลูปนำเข้าหลายไฟล์ ให้ try/catch ต่อไฟล์ ไม่ใช่ครอบทั้งลูป (ไฟล์เสียใบเดียวไม่ควรล้มทั้งชุด) ·
  เช็ค mime แล้ว fallback นามสกุลด้วย — blob ที่ผ่าน IndexedDB มา `type` อาจว่าง
